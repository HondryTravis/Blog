<!-- 红色块级提醒文字 -->
export const Red = ({children, color}) => (
  <p style={{color: color ? color : 'red'}}>
    {children}
  </p>
);

<!-- 红色内联提醒文字 -->
export const RedInline = ({children, color}) => (
  <span style={{color: color ? color : 'red'}}>
    {children}
  </span>
);

export const Rect = ({url, left}) => (
<span style={{
  display:'inline-block',
  height: '20px',
  width: '20px',
  marginLeft: `${left ? left : 0}px`,
  background: `url(${url})`,
  backgroundSize: 'cover'
  }}>
</span>
)
